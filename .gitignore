.DS_Store
.env
.env.local
*.log

.venv/
__pycache__/
*.pyc

node_modules/
dist/
build/
docs/.vitepress/cache/
docs/.vitepress/dist/

.claude/settings.local.json

# Go build output (CI rebuilds; not checked in)
bin/

# Worker-agent internal state — per-spawn scratch, not for main
.worker_agent/

# agent-teamd runtime: pidfile, unix socket, daemon log, per-instance metadata.
# Definitions live in `.agent_team/agents/` (committed); runtime state is local-only.
.agent_team/daemon.pid
.agent_team/daemon.sock
.agent_team/daemon/
.agent_team/state/
